/* eslint-disable prettier/prettier */
import {StyleSheet} from 'react-native';

export const textinput_style = StyleSheet.create({
  inputStyle: {
    backgroundColor: '#8f8e8e',
    height: 40,
    borderRadius: 6,
    marginTop: 18,
    borderWidth: 0,
    borderBottomWidth: 0, // Remove the bottom line
    fontSize: 20,
    padding: 3,
  },
  placholderStyle: {
    fontSize: 18,
    textTransform: 'uppercase',
    marginBottom: 0,
    color: '#000000',
  },
  inputTitle: {
    textTransform: 'uppercase',
    fontSize: 18,
    fontWeight: 'bold',
  },
  mapInputStyle: {
    width: '80%',
    marginTop: 20,
    backgroundColor: '#8f8e8e',
    borderRadius: 6,
    borderWidth: 0,
    borderBottomWidth: 0, // Remove the bottom line
    fontSize: 20,
    padding: 3,
  },
});
